 Project Description

This project implements the Scaled Dot-Product Attention mechanism using Python.

The project uses sentence embeddings generated using the
`all-MiniLM-L6-v2` model from Sentence Transformers.

The attention mechanism calculates Query (Q), Key (K), and Value (V)
matrices and generates attention weights and final attention outputs.

